'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.logout = logout;

var _utils = require('../utils');

var helpMessage = '\nUsage:\n\n  reaction logout\n';

function logout(yargs) {
  _utils.Log.args(yargs.argv);

  var args = yargs.argv;

  if (args.help) {
    return _utils.Log.default(helpMessage);
  }

  _utils.Config.unset('global', 'launchdock');

  _utils.Log.success('Successfully logged out.');
}